# Run 1 — 2026-09-22

Instruction from Rae: go into `/dev`, start working on bettercomputer, design a new phone as
files and code, install what is needed, keep a file-based state tracker for cold starts.

## Timeline

- Read the existing repo (August .NET scaffold, product vision) and the sibling `termphone`
  repo to avoid designing the same phone twice.
- Created the Python 3.11 venv with CadQuery + build123d (background install).
- Wrote `CLAUDE.md`, `project/` tracker, `.gitignore`.
- (continued below as units complete)
- Rae: "prioritise planning". Wrote roadmap (M0 P1–P12, M1–M3), design path, product README,
  principles, personas (flagged [UNVERIFIED]), requirements (40 REQ ids), decision points (14),
  risk register (10), unit plan (U1–U15).
- Rae: "set up a mailbox for the agents". Built `/Users/raejeong/agentmail` (file-backed queue +
  localhost HTTP + CLI). launchd persistence denied by policy; server runs under nohup. Peers
  second-brain, termphone, consolegenie joined; two-lane convention agreed (live: cross-session
  SendMessage; durable: agentmail).
- Rae: "wrap up for a model switch and /clear". State, plan, checker and cold-start block written
  so the next session can resume at U3 with no chat history.

## Handoff

Next unit: **U3** (`phone/spec/device.yaml`, `phone/spec/bus.md`, ADRs 001 and 002). Nothing
blocked. Venv exists for U7.
