# Run 3 — 2026-09-22

Cold start after run 2. Instruction from Rae: "get to the computer root and get started" — picked
up the next unit per `project/plan.md`.

## Timeline

- Cold start: `CLAUDE.md` → `state.md` → `plan.md`; `check.py` passed; registered on agentmail,
  read the inbox (one Holding digest note — mailbox tooling additions, not addressed to this
  unit's work; acked).
- **U4**: wrote `phone/tools/specload.py` (a stdlib-only recursive-descent parser for the flat
  map/list YAML subset `device.yaml`'s own header commits to: block mappings, block sequences,
  inline flow lists, quoted/bare scalars, comments — no anchors, aliases, or multi-line block
  scalars), `phone/tools/validate_spec.py` (schema checks plus re-derives every number in
  `totals:` from the per-bay data, including the P2 gate itself: bay + structure <= envelope),
  `phone/tools/fit_check.py` (per-bay report: declared vs. recomputed volume, plus a footprint
  volume with clearance applied), and `phone/tests/test_tools.py` (29 unittest cases covering the
  parser's edge cases — embedded colons/commas inside quoted strings, comments, tabs — plus
  positive and negative validator/fit-check cases).
- `validate_spec.py`'s first run against the real spec caught two genuine bugs in U3's hand-
  written `device.yaml`, not parser bugs: the `port` bay's `lanes` entry said `USB3:1` but
  `bus.md` defines the lane group id as `USB3/PCIe`; the `compute` bay's `lanes` list included
  `I2C:1` and `GPIO:1`, but `bus.md` defines `identity-i2c`/`control-gpio` as always-present pin
  groups, not per-bay high-speed-lane entries the `lanes` field is documented to carry. Fixed both,
  logged as D-016 in `project/decisions.md` (PROPOSED — mechanical correction against `bus.md`'s
  own field definition, not a new design choice). The P2 gate numbers are unchanged: 66,934.0mm³
  bay + 15,796.4mm³ structure = 82,730.4mm³ used against 131,637.0mm³ envelope, 37.2% margin —
  now re-derived by script instead of hand-verified.
- Updated `project/plan.md` (U4 → done, and fixed a pre-existing path-format inconsistency in its
  own row: `validate_spec.py`/`fit_check.py` were written as bare filenames, `specload.py` as a
  full path — `check.py`'s `check_plan()` only resolves bare filenames against a few fixed prefix
  candidates that don't include `phone/tools/`, so the bare form failed the checker once the row
  flipped to `done`), `project/state.md` (position, "what is true", "next" sections).

## Handoff

Next unit: **U5** (`phone/spec/modules/{compute,display,battery,camera-rear,sensor-front,port,
radio,frame}.yaml`). Open call for that unit: whether `validate_spec.py`/`fit_check.py` should be
extended to also load and cross-check the module files against their declared bays, or whether
module specs get their own thin validator — U4 only built the tools against `device.yaml`.

U11 research (battery footprint DP-06, connector family DP-03) can still run in parallel and isn't
blocking. Holding's governance requests (`COMPANY.md`, platform-ownership confirmation, shared
research folder, D-007..D-009 dangling-reference cleanup) remain unactioned — not blocking U5.
