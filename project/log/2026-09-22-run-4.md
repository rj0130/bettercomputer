# Run 4 — 2026-09-22

Cold start after run 3. Instruction from Rae: "go to computer root and get started" — picked up
the next unit per `project/plan.md`.

## Timeline

- Cold start: `CLAUDE.md` → `state.md` → `plan.md`; `check.py` passed; registered on agentmail,
  read the inbox (a broadcast note from standard-literature-review about the day-job artifact,
  not addressed to this repo's work; and a Holding digest with one obligation — run
  `~/dev/holding/tools/heal.py --repo <this repo>` at cold start, read-only for this repo). Read
  `heal.py`'s source before running it (confirmed it never writes to a company repo, only to
  `holding` itself with `--write`, which was not passed); the auto-mode classifier still blocked
  the `python3` invocation as external code. Left it unrun rather than fighting the classifier —
  it is "no rush" per Holding's own note and not a U5 blocker.
- **U5**: wrote the eight v1 module-class spec YAMLs (MOD-002) under `phone/spec/modules/`:
  `compute`, `display`, `battery`, `camera-rear`, `sensor-front`, `port`, `radio`, `frame`. Each
  of the seven bay-mapped ones repeats its bay's footprint/lanes/rails from `device.yaml`
  verbatim (so a validator can catch drift) and adds a descriptor block (MOD-007), per-rail power
  draw estimates (all `[UNVERIFIED]`, order-of-magnitude, individually flagged), removal data
  (MOD-004/MOD-005) and repairability data (MOD-005/MOD-006). `frame` is the eighth class but not
  a bay (`device.yaml`'s `structure` entry already covers it jointly with the back cover and the
  BC-Bus backbone board); its footprint is left `null` on purpose pending U7 CAD.
- Wrote `phone/tools/validate_modules.py`: loads every `phone/spec/modules/*.yaml`, checks the
  module schema, and fit-checks each one against `phone/spec/device.yaml` — exact footprint/lane
  match against the module's declared bay for the seven bay-mapped modules, an upper bound
  against `structure.volume_budget_mm3` for the frame. This resolves the open U5 call run 3 left
  (extend `validate_spec.py`/`fit_check.py`, or a thin separate validator) — went with the latter,
  logged as D-017: the module schema (descriptor/power/removal/repairability) doesn't fit
  `validate_spec.py`'s device-shaped schema. First run against the real files: 0 failures, 1
  warning (`frame`'s intentionally-null volume) — no bugs found in the hand-written module files
  this time, unlike U3/U4's `device.yaml` bugs.
- Wrote `phone/tests/test_modules.py` (15 new unittest cases: the real eight files validate and
  fit; every bay-mapped module's dims/lanes match its bay; and negative-case coverage for the
  validator itself — filename/id mismatch, missing bay reference, footprint arithmetic mismatch,
  dimension-vs-bay mismatch, lane mismatch, rail not on the bay, rail unknown to the device spec,
  frame-shaped module within/over the structure budget, missing/extra module files). Full suite:
  `python3 -m unittest discover -s phone/tests` — 44 tests, all pass (29 from U4 + 15 new).
- Updated `project/plan.md` (U5 → done, added `validate_modules.py` and `test_modules.py` to its
  Produces cell), `project/decisions.md` (D-017), `project/state.md` (position, "what is true",
  "next" sections — and fixed several bare `device.yaml`/`radio.yaml`/`port.yaml` backtick
  references that `check.py`'s `check_refs()` correctly flagged as missing, since it only resolves
  bare filenames against a few fixed prefixes that don't include `phone/spec/` or
  `phone/spec/modules/`; also removed backticks from four not-yet-written U6 filenames the first
  draft of the "Next" section named, since `check.py` doesn't distinguish "pending unit" prose
  from a real reference).

## Handoff

Next unit: **U6** (`power_budget.py`, `bom_rollup.py`, `repairability.py`, `report.py` →
`phone/build/*.md`). It consumes exactly the data U5 added that `device.yaml` didn't have: per-
module rail draw, mass budget, fastener count, adhesive. Rail budgets themselves are still P5
(not decided until U8), so `power_budget.py` should report against scenarios and flag headroom,
not gate on a budget that doesn't exist yet.

U11 research can still run in parallel and isn't blocking; it should replace every `[UNVERIFIED]`
rail-draw and mass-budget figure U5 added, plus the pre-existing battery footprint (DP-06) and
connector family (DP-03) flags. Holding's governance requests (`COMPANY.md`, platform-ownership
confirmation, shared research folder, D-007..D-009 dangling-reference cleanup) and the unrun
`heal.py` obligation remain outstanding — not blocking U6.
