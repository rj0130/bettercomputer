# Run 5 — 2026-09-23

Cold start after run 4. Instruction from Rae: "go to document root and get started" — picked up
the next unit per `project/plan.md`.

## Timeline

- Cold start: `CLAUDE.md` → `state.md` → `plan.md`; `check.py` passed clean; registered on
  agentmail, read the inbox. Three new notes since run 4, none blocking U6: an alignment note
  (day-job/McMaster-Carr artifact work stays off this Mac, not this repo's concern), and two
  Holding digests — obligation 5 (D037, `heal.py` at cold start) repeated with no new action, and
  Rae's live-board answers to A001/A002 (KiCad install authorized when a unit needs a board
  layout — not yet; push to `origin` authorized in principle but gated on Rae saying "push" in
  the live thread first — no push happened, none should until that word arrives). `heal.py`
  remains unrun, same status as run 4.
- **U6**: wrote the four P3 tools `project/plan.md` names — `phone/tools/power_budget.py`,
  `bom_rollup.py`, `repairability.py`, `report.py` — and read `phone/spec/modules/*.yaml`,
  `phone/tools/{fit_check,validate_modules,validate_spec}.py` and `phone/design/design-path.md`'s
  P3/P9/P10 gate text first to see what data actually exists to report on before writing anything.
  Key finding: design-path.md's P3 method line says each module YAML should carry a "cost
  estimate with flag," but U5 never added a `cost` field — evidence hygiene (CLAUDE.md: never
  invent a price) meant `bom_rollup.py` reports the BOM as **not computable** (0/8 modules have
  cost data) rather than inventing per-module component prices the way U5 did for power/mass.
  Logged as D-019; real costing stays P10/U13 as design-path.md already says.
  - `power_budget.py`: four scenarios (idle, screen-on, video, 5g-data), each assigning every
    module an "off"/"typical"/"max" load level from a hand-authored `SCENARIOS` table — not
    sourced from any requirement, since no per-scenario power model exists until U10 and no real
    chipset numbers exist until U11. Flagged in the script's own docstring and in D-018. Sums
    per-rail mA from each module's `power` block; `source` (battery) and `passthrough` (port's
    VBUS charging input) entries are correctly excluded from rail-load totals. No rail carries a
    budget yet (P5/U8), so nothing here gates — it only reports, same as state.md's run-4
    handoff asked for.
  - `bom_rollup.py`: mass rollup sums `mass_budget_g` across the seven modules that have it
    (114.0g), excludes `frame` (still `null`, pending U7 CAD) from the total rather than treating
    it as zero, and reports 101.0g of *provisional* headroom against the 215g ceiling — not a
    confirmed PASS, since the missing frame mass could still push the real total over. It does
    fail loudly (exit 1) if the modules-only total alone already exceeds the ceiling, mirroring
    `fit_check.py`'s envelope gate; that didn't trigger against the real spec. Cost rollup: see
    above, not computable (D-019).
  - `repairability.py`: REQ REG-003 asks for "a repairability score computed by the same rubric
    as the EU index," but the real EU/French index also scores documentation, spare-parts
    availability and spare-parts price — none of which exist as spec data yet (P6/P9/P10). Rather
    than fake those three, this script computes a **disassembly-only** sub-score out of 10 per
    module (tool, fastener count linearly scaled 0–6, adhesive, removal-order dependency), says
    explicitly that it is not the full index, and leaves REG-003 open for P9/U12 (logged D-020).
    Device average across the 7 bay-mapped modules (frame excluded — MOD-005 already carves it
    out as "removed last... full teardown"): 8.57/10. The rubric correctly re-derives, from spec
    data alone, the two known nuances earlier runs had already hand-flagged: `radio.yaml`'s RF
    coax pigtail removal-order dependency scores 0/2 and is flagged; `frame.yaml`'s "full
    teardown" tool and `null` fastener count both score 0 and are flagged rather than guessed.
  - `report.py`: imports the other three plus `fit_check.py`, renders each to markdown, and
    writes `phone/build/{fit,power-budget,bom,repairability,report}.md` — the first files under
    `phone/build/`. `report.md` is an index that states design-path.md's P3 gate ("report.py
    produces fit, mass, power, cost and repairability reports with zero failures; tests pass")
    explicitly and reports it PASS. Re-running it after any spec change is meant to be routine;
    the files are marked "do not hand-edit."
- Wrote `phone/tests/test_reports.py`: 32 new unittest cases — function-level tests for each
  script's core logic (scenario rail totals incl. source/passthrough exclusion and off-by-default
  modules; mass/cost rollup incl. missing-data handling; each of the four rubric sub-scores incl.
  the D-004 tool-drift flag and the fastener linear scale; `report.py`'s render functions), plus
  two integration tests that call `main()` directly against tempdirs: `bom_rollup.main()`'s
  mass-ceiling-exceeded exit-1 path (synthetic device spec with an artificially low ceiling) and
  `report.main()`'s five-file output. Full suite: `python3 -m unittest discover -s phone/tests` —
  76 tests, all pass (44 carried over from U4/U5 + 32 new). One test bug caught and fixed during
  this run: the first draft of `test_module_score_totals_out_of_ten` assumed 3 fasteners scored
  full marks on a threshold basis; the actual rubric is a linear scale, so the fixture was wrong,
  not the rubric — fixed the fixture to use 0 fasteners for a clean 10/10 case.
- Updated `project/plan.md` (U6 → done; added `phone/tools/` prefixes to the four script names in
  its Produces cell — the pending-status draft had them bare, which `check.py`'s `check_plan()`
  would have failed to resolve once marked done; added `phone/tests/test_reports.py`),
  `project/decisions.md` (D-018 scenario-model assumption, D-019 BOM-not-computable, D-020
  disassembly-only rubric), `project/state.md` (position, unit table, "what is true", "next" —
  and fixed a batch of bare-filename backtick refs the first draft introduced, e.g. `fit.md`
  instead of `phone/build/fit.md`, `device.yaml` instead of `phone/spec/device.yaml`, that
  `check.py`'s `check_refs()` correctly flagged as missing since it only resolves bare names
  against a few fixed prefixes; also dropped backticks from U7's not-yet-written filenames,
  same fix run 4 made for U6's).

## Handoff

Next unit: **U7** (P4, mechanical): CadQuery frame generator, module dummies, exploded-view SVG,
driver script (`project/plan.md`'s U7 row has the exact filenames) → STEP/STL/SVG exports plus
`phone/hardware/fasteners-and-tolerances.md`. Needs the already-installed `.venv/` CadQuery
environment (D-005/D042). This is the unit that finally gives the frame its own real volume and
mass, closing the `null` placeholders `validate_modules.py`, `bom_rollup.py` and
`repairability.py` have all been carrying forward since U5.

U8 (P5, electrical) can start in parallel — it is what actually sets rail budgets, the gap
`power_budget.py` deliberately left unfilled this run, and it owns resolving `radio.yaml`'s RF
coax pigtail removal-order nuance that `repairability.py` now scores as a real flag rather than
leaving it as prose.

U11 (research) remains the standing parallel track: every `[UNVERIFIED]` flag from U5 (rail draw,
mass budget, battery footprint DP-06, connector family DP-03) is still open, and it is now also
the source that should let a real `cost` block land in each module YAML so `bom_rollup.py` stops
reporting the BOM as not computable (D-019).

Holding's standing asks (COMPANY.md, platform-ownership confirmation, shared research folder,
D-007..D-009 dangling-reference cleanup, the unrun `heal.py` obligation) remain outstanding, not
blocking U7. This run's mailbox also logged Rae's live-board answers on A001 (push — wait for the
literal word "push" in the thread) and A002 (KiCad — install only when a unit needs board layout);
neither is actioned yet since neither condition has been met.
