# BetterComputer Architecture Scaffold

This folder contains skeletal projects for the BetterComputer platform: Shared libs, multiple Web APIs (Orders, Inventory, Staff, Operations, RnD, Management), and a Workers host.

Run `dotnet restore` and add projects to the solution with `dotnet sln add` as needed, or open the folder in VS Code and use the solution explorer extension.
